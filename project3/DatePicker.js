'use strick';

class DatePicker {
    constructor(id, callback) {
        this.id = id;
        this.callback = callback;
    }

    render(date) {
        // 1. create a div and add it to the document.
        
        // 2. add a table in the div.

        // 3. set the contents of the table.

    }
};

